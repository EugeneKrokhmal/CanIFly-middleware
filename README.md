# @canifly/middleware

Shared types, Zod schemas, geo helpers, and EN/ES domain labels for CanIFly.

```bash
npm install
npm run build
```

Local consumers:

```json
"@canifly/middleware": "file:../CanIFly-middleware"
```
